TODO:

The EOWYNE whiteboard ...
-	rewrite menus so that you can link menubar by name
-	rewrite rules so that a folders permissions apply to its children.
-	utility.generator.randomkey sollte dahingehend verbessert werden, das nur
	unique keys rauskommen